---

- name: "Setup Laptop"
  hosts: all
  roles:
    - role: homebrew
      tags:
       - homebrew
    - role: osx_defaults
      tags:
       - defaults

  tasks:
  - name: Create ~/workspace
    file:
      dest: "~/workspace"
      state: directory
  - name: Create ~/.ssh
    file:
      dest: "~/.ssh"
      state: directory
